# native/

- iOS: RealityKit ARView shell (PR 4 / 4a)
- Android: ARCore + Filament (or GLES fallback) per Android rendering annex (PR 5a / 5)

Flutter PlatformView embeds the native AR scene; world-locked content is never positioned solely from late Dart poses.
